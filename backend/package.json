{
  "name": "re_digest",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" ",
    "dev:frontend": "vite",
    "dev:backend": "node python-script-server.js",
    "dev:clear-outputs": "echo '' > src/outputs/restriction-digest-analysis.txt",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
